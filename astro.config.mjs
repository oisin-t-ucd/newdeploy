// @ts-check
import { defineConfig } from "astro/config";


// https://astro.build/config
export default defineConfig({
  site: "https://oisin-t-ucd.github.io/", // this should be your main github pages URL, following the format username.github.io
  base: 'newdeploy', // this should be set to the name of your github repository
});
